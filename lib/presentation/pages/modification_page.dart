import 'package:flutter/material.dart';

class ModificationPage extends StatelessWidget {
  const ModificationPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Modifcation page'),
    );
  }
}