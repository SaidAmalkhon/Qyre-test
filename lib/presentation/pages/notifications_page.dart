import 'package:flutter/material.dart';

class NotificatinPage extends StatelessWidget {
  const NotificatinPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Notification page'),
    );
  }
}